# 2026-05-27 10:45 :: sync FreeCounting research state into GOC repo

scope: codes/, library/notes/, results/, history/, history.md, AGENTREAD.md, codes/index.md, library/index.md, results/index.md

summary:
- User asked to bring all research artifacts from `/home/czp/ws_yiyang/FreeCounting/` (a parallel working copy with the actual OCCAM reimplementation, the SNG ablation results, and the workspace-root `Model_innovation.md` write-up) into this `GOC-Freely-Object-Counting/` repo so a future agent can continue independently.
- Performed a full sync (option "full"): copied OCCAM package + scripts + evaluator into `codes/`, copied the SNG method write-up + upstream READMEs into `library/notes/`, copied 6 evaluation/ablation runs into `results/<run_id>/` (one per logical campaign), and authored config.yaml + README.md for each run. Updated codes/index.md, library/index.md, results/index.md, history.md (+ history/), and rewrote AGENTREAD.md §6 current task to reflect the new state and explicit next steps.
- After sync, the next agent has: a runnable baseline, a written-up clustering contribution with theoretical analysis, 6 reproducible runs with metrics, and a roadmap (§7.1–§7.5 of SNG-method.md, plus the open MP7 anomaly).

key_decisions:
- Run-id naming followed `results/index.md` ENTRY_FORMAT (`YYYY-MM-DD-HHMM-<task>-<short_tag>`); used the launch / "Started" timestamps from each FreeCounting summary.txt where available, otherwise the directory mtime.
- For ablation campaigns with multiple sub-configs (mask-area, mask-policy, clustering), kept the upstream `results/<sub_run>/` substructure inside the GOC run folder rather than re-flattening — preserves the 1-to-1 mapping back to FreeCounting and avoids huge per-image JSON duplication.
- For single-config runs (FSC-147 single + multi baselines, the latter previously wrapped in `occam_multi/results/`), flattened so `metrics.json` sits at the run-folder top level, matching results/index.md LAYOUT.
- All ported code is byte-for-byte (single intentional change: `eval_fsc147_full.py` PROJECT_ROOT path adjusted from `parents[2]/"OCCAM"` to `parents[1]` to fit the new `codes/eval/` location). Documented in the matching history entry.
- Did NOT copy the Excel into a per-run folder; placed the cross-run summary at `results/OCCAM_experiment_results.xlsx` and noted it in `results/index.md` tree.
- Updated AGENTREAD.md §6 with concrete numbers, the SNG sweet-spot finding, the MP7 anomaly, and a numbered next-steps list — so the next agent has a precise starting point without re-reading the entire results tree.

files_touched:
- codes/occam/* :: created :: ported OCCAM baseline package + README + requirements.
- codes/scripts/{run_occam,eval_omnicount}.py :: created :: ported single-image and OmniCount CLIs.
- codes/eval/{eval_fsc147_full,aggregate_excel}.py :: created :: ported full FSC-147 evaluator + cross-run XLSX aggregator.
- library/notes/{SNG-method,freecounting-original-readme,occam-impl-original-readme}.md :: created :: synced upstream docs.
- results/2026-05-{11-0703,17-0152,17-1138,19-1015,20-0942,21-1328}-* :: created :: 6 run folders (1 single-baseline, 1 multi-baseline, 4 ablation campaigns) with config.yaml + README.md authored per run.
- results/OCCAM_experiment_results.xlsx :: created :: cross-run summary.
- codes/index.md :: modified :: registered occam/, scripts/, eval/ trees.
- library/index.md :: modified :: registered notes/ subfolder + 3 files.
- results/index.md :: modified :: appended 6 run rows + tree update.
- history.md :: modified :: appended sync entry pointing to history/2026-05-27-1045-sync-occam-and-results.md.
- history/2026-05-27-1045-sync-occam-and-results.md :: created :: full structured change record with operations_delta + followups.
- AGENTREAD.md :: modified :: §6 current task rewritten with concrete state (baseline numbers, SNG sweet spot, MP7 anomaly) + 6-step actionable plan.
- memory/index.md :: modified :: appended this entry.

followups:
- next agent: install runtime deps and re-run the OCCAM-S baseline to verify the synced numbers reproduce (MAE val ≈ 43.65 on FSC-147 val 1/3 subset). Use `python codes/eval/eval_fsc147_full.py --mode single --splits val --fraction 0.333 --seed 42 --output-dir results/<new_run_id>/`. SAM2 must be installed separately.
- next agent: implement §7.1 of `library/notes/SNG-method.md` (adaptive δ via α ∈ [0.3, 0.5]) — lowest cost, biggest expected payoff (cross-dataset robustness). Hypothesis: η pinned at 0.4–0.5 keeps MAE within ±1 of the manually-tuned best across FSC-147 / CARPK without retuning. Document hypothesis in a NEW memory file BEFORE launching, log code change under history/, register run in results/index.md.
- next agent: investigate the MP7 anomaly (no project-side mask filter beats P0 paper baseline by ~1 MAE on multi-mode). Verify on FSC-147 test split first (currently only val 1/3 is measured); if it holds, this is itself a publishable finding ("AMG's internal NMS is sufficient; downstream filters remove signal").
- next agent: do NOT delete any of the synced runs or the FreeCounting source tree; both are reference baselines and the FreeCounting tree may still be used as a scratch area by the user. If the user later asks to drop FreeCounting/, double-check that every results/ folder + library/notes/ file is still self-contained first.
