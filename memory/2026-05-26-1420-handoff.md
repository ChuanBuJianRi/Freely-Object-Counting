# 2026-05-26 14:20 :: handoff snapshot for next agent

scope: whole repo (AGENTREAD.md, library/, codes/, results/, tmp/, memory/, history/, skills/)

summary:
- This is a HANDOFF memory. The next agent should read this first, then `AGENTREAD.md`, then the most recent prior memory entries.
- The previous agent (this session) bootstrapped the entire agent-readable contract for this repo and installed/authored a complete skill stack. No project code has been written yet — the contract and tooling are in place; the next concrete step is scaffolding `codes/` and reproducing the OCCAM baseline.
- Project = GOC-Freely-Object-Counting. Goal: training-free / label-free / class-free open-world object counting. Baseline = OCCAM (PDF at `library/paper/OCCAM.pdf`, arXiv 2601.13871). Active task: replace/augment OCCAM's internal modules to improve counting accuracy while preserving training-free / label-free / class-free properties.

state_of_repo (as of 2026-05-26 14:20):
- top-level :: AGENTREAD.md, README.md, LICENSE, history.md, codes/, library/, results/, tmp/, memory/, history/, skills/
- codes/ :: only contains index.md; NO project code yet. Skeleton mirroring OCCAM stages (proposals / features / matching / aggregation) is the next concrete step.
- library/paper/ :: OCCAM.pdf (the baseline paper). No notes file yet.
- library/scripts/ :: empty.
- results/ :: only index.md; no runs yet.
- tmp/ :: only index.md; empty.
- memory/ :: index.md + 4 entries (1328 bootstrap, 1358 install skills, 1408 author local skills, 1420 this handoff).
- history/ :: 2 entries (1358 install-claude-skills, 1408 author-local-skills).
- skills/ :: 12 skills installed/authored, with index.md (see below).

agent_readable_contract (the system the next agent MUST follow):
- AGENTREAD.md is the project entry point. Section 4 lists every file that MUST be kept up to date with explicit triggers.
- per-folder index files own the detailed UPDATE_RULES:
  - library/category.md :: papers + scripts catalog.
  - codes/index.md :: code units with `purpose`/`operations`/`entrypoint`/`usage`/optional `inputs`/`outputs`.
  - results/index.md :: one row per run; layout `results/<run_id>/` with `config.yaml`/`metrics.json`/`log.txt`/optional `checkpoints`/`vis`/`predictions`/`README.md`. run_id = `YYYY-MM-DD-HHMM-<task>-<short_tag>`.
  - tmp/index.md :: every temp file with `created_by` + `expires`; never long-lived.
  - memory/index.md :: chunks every ~3-5 prompts or topic shift; flat path `memory/YYYY-MM-DD-HHMM.md`.
  - history.md + history/ :: append-only code-change log; one md per logical change at `history/YYYY-MM-DD-HHMM-<tag>.md`. NEVER delete past entries — log reverts as a NEW entry.
  - skills/index.md :: external + local skills, one row each.
- entry separator across all index files :: ` :: ` (space-colon-colon-space).
- timestamps :: `YYYY-MM-DD HH:MM` (24h, local, zero-padded).
- file-name tags :: kebab-case, ≤24 chars.
- style :: lowercase keys, fixed fields, one-line entries; prose belongs in dedicated files (memory / history / per-run README).

skills_inventory:
- external (6) ::
  - pdf :: anthropics/skills :: PDF merge / form-fill / image extraction.
  - frontend-design :: anthropics/skills :: project sites, demos, dashboards.
  - skill-creator :: anthropics/skills :: meta-skill, used to author the local skills below.
  - pdf-reading-to-markdown :: aliceisjustplaying :: full PDF -> markdown.
  - pdf-reading-split :: scunning1975/MixtapeTools :: 4-page chunk deep-read for papers.
  - pdf-reading-quantum :: SPA3K :: skim/scan large docs, ~40% token savings.
- local (6, all authored this session) ::
  - counting-eval :: standardized counting eval (MAE/RMSE/NAE) -> `results/<run_id>/eval/`.
  - paper-reading :: 6-section structured paper notes -> `library/paper/<stem>.notes.md`.
  - ablation-runner :: A×B×C matrix -> configs/ + run_grid.sh + parse_logs.py + table.md/.tex.
  - pseudo-label-pipeline :: SEEM/SAM -> GMM -> NMS -> 2× refinement, with VRAM probe + checkpoint resume.
  - experiment-logger :: enforce `exp_YYYYMMDD_<dataset>_<method>_<tag>` naming + write experiment.md.
  - figure-maker :: paper-ready figures with locked style (Times/Arial, color-blind palette, ≥300 DPI, vector PDF).
- file-reading skill was REQUESTED but DEFERRED — no canonical source found. If needed, author locally via skill-creator.

key_decisions_made_this_session (do NOT silently undo):
- index files are agent-readable: lowercase keys, fixed fields, one-line entries, ` :: ` separator, no prose paragraphs.
- memory layout is FLAT (`memory/YYYY-MM-DD-HHMM.md`), not nested by date — switched explicitly per user.
- history is APPEND-ONLY; reverts go in as new entries, never delete.
- skills installed at PROJECT-ROOT `skills/`, not `.claude/skills/` (Cursor agents read it as docs; Claude Code auto-loading would require a symlink, deferred).
- 3 community pdf-reading variants kept side-by-side (`pdf-reading-to-markdown`, `pdf-reading-split`, `pdf-reading-quantum`) so user can pick after trying; do NOT delete without user confirmation.
- All local skill `description` fields are intentionally "pushy" (e.g. "ALWAYS invoke when ...") to fight Claude's under-trigger tendency.
- Local skills hard-wire output paths to existing index contracts (counting-eval/ablation-runner/experiment-logger -> results/index.md; paper-reading -> library/category.md; figure-maker scripts -> codes/index.md), preserving single-source-of-truth.
- figure-maker's style contract is HARD: deviations require explicit user override AND must be recorded in the figure caption file.

cross_folder_obligations (the next agent MUST honor):
- code change altering operations :: update codes/index.md AND log a history/ entry (with operations_delta).
- new experiment run :: create results/<run_id>/ + append row to results/index.md; reference matching history/ file in run README.md.
- promote a tmp/ file :: delete its row from tmp/index.md + add to destination folder index.
- close a memory chunk :: list files_touched and verify each is reflected in codes/index.md, history.md, results/index.md as appropriate.

open_questions_for_next_agent:
- Should `skills/` be symlinked to `.claude/skills/` so Claude Code auto-loads them? (currently only Cursor agents see it as docs)
- Does the user want the `file-reading` skill authored locally, or stay deferred?
- After trying the three pdf-reading-* variants, which one(s) to keep? (do NOT remove without user confirmation)

next_concrete_steps (carried over from AGENTREAD.md §6):
1. scaffold `codes/` mirroring OCCAM's stages so individual modules can be swapped:
   - codes/proposals/  (SAM-style class-agnostic mask proposals)
   - codes/features/   (CLIP / DINOv2 / SigLIP feature extractors)
   - codes/matching/   (text/exemplar prompt -> proposal scoring)
   - codes/aggregation/(noisy-proposal -> count, clustering / NMS / refinement)
   - codes/eval/       (entrypoints that hand off to counting-eval skill)
   - codes/figs/       (entrypoints that hand off to figure-maker skill)
   Each new file/module MUST be registered in codes/index.md with `purpose` + `operations`.
2. reproduce OCCAM baseline numbers on at least ONE benchmark (FSC-147 val recommended). Use experiment-logger BEFORE launching, record under results/<run_id>/, evaluate via counting-eval. This run is also the smoke test for the new skill stack.
3. structurally read OCCAM via paper-reading skill, output to library/paper/OCCAM.notes.md. Update library/category.md.
4. pick ONE module axis for the first ablation (proposals / features / matching / aggregation). Document hypothesis in a NEW memory file BEFORE launching the ablation. Use ablation-runner.
5. log every code change under history/, every experiment under results/, every conversation chunk under memory/. Use figure-maker for any plot.

operating_constraints (do NOT relax without explicit user approval):
- training-free :: no fine-tuning / gradient updates on counting datasets.
- label-free :: no use of dataset GT during inference.
- class-free :: no fixed class list; queries arrive as text prompt or exemplar.
- reproducibility :: every run goes into results/<run_id>/ with config.yaml + metrics.json + log.txt + experiment.md (latter via experiment-logger).
- user requested ALL communication in Simplified Chinese; tool outputs and file contents stay in English by project convention, but assistant prose to the user is in Simplified Chinese.

how_to_resume:
1. read AGENTREAD.md fully (esp. §4 must-update table and §6 current task).
2. read this file (memory/2026-05-26-1420-handoff.md) and the prior 3 memory entries (1328, 1358, 1408).
3. read skills/index.md to know what tools you already have.
4. read codes/index.md and results/index.md to confirm they are still empty (should be).
5. then either start step 1 above (scaffold codes/) OR ask the user to confirm direction.

key_decisions:
- this is a HANDOFF entry, intentionally larger than the per-chunk template, because it must let a fresh agent reconstruct full context without rereading every file.

files_touched:
- memory/2026-05-26-1420-handoff.md :: created :: this handoff snapshot.
- memory/index.md :: modified :: appended entry row for the handoff.

followups:
- next agent: confirm with user before deviating from any contract listed above.
- next agent: do not delete the three pdf-reading-* variants, the file-reading deferral, or any history entry without explicit user instruction.
