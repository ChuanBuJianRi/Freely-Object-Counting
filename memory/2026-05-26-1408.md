# 2026-05-26 14:08 :: author 6 local project-specific skills

scope: skills/, history/, memory/

summary:
- Authored 6 project-specific Claude Skills covering the repeated workflows in this research project.
- counting-eval :: standardized counting evaluation (MAE/RMSE/NAE) on FSC-147 / CARPK / ShanghaiTech, fixed output layout under `results/<run_id>/eval/`.
- paper-reading :: structured 6-section paper notes (Problem / Method / Key Insight / Experiments / Limitations / Relation to my work) extracting ALL numerical results so comparison tables stitch automatically later.
- ablation-runner :: from a config-matrix description ("A × B × C") generate configs/, run_grid.sh, parse_logs.py, and the final ablation `table.md` + `table.tex`; cap at 64 cells.
- pseudo-label-pipeline :: encode SEEM/SAM → GMM → NMS → 2× refinement with VRAM auto-probe, per-image checkpointing, and resume-from-failure.
- experiment-logger :: enforce `exp_YYYYMMDD_<dataset>_<method>_<tag>` naming and write `experiment.md` (git sha, config diff, seed, hw, timing, metrics) before any training run.
- figure-maker :: lock paper-figure style (Times/Arial, color-blind palette, ≥300 DPI, vector PDF) for density maps, qualitative grids, curves, ablation bars, GT-vs-Pred scatter.
- Updated `skills/index.md` :: split into `## external skills` and `## local (project-specific) skills`, refreshed `## suggested usage in this project`.
- Logged this change in `history/2026-05-26-1408-author-local-skills.md` and registered in `history.md`.

key_decisions:
- All 6 are authored as LOCAL skills (no upstream); each `SKILL.md` has YAML frontmatter `name` + `description` per anthropics/skills convention; descriptions intentionally "pushy" to fight under-triggering.
- Skill bodies fix output paths to the project's existing contract (`results/<run_id>/`, `library/paper/`, `codes/figs/`) so the skills compose with `results/index.md`, `codes/index.md`, `library/category.md` rules already in place.
- counting-eval, ablation-runner, experiment-logger explicitly cross-update `results/index.md`; paper-reading cross-updates `library/category.md`; this preserves the single-source-of-truth principle.
- figure-maker imposes a HARD style contract (font / palette / DPI / legend / no chart-junk) regardless of user phrasing; deviations require explicit override recorded in the figure caption file.
- Naming convention `exp_YYYYMMDD_<dataset>_<method>_<tag>` is now enforceable, not just suggested.

files_touched:
- skills/counting-eval/SKILL.md :: created.
- skills/paper-reading/SKILL.md :: created.
- skills/ablation-runner/SKILL.md :: created.
- skills/pseudo-label-pipeline/SKILL.md :: created.
- skills/experiment-logger/SKILL.md :: created.
- skills/figure-maker/SKILL.md :: created.
- skills/index.md :: modified :: split external/local, added 6 local skills, expanded suggested usage.
- history/2026-05-26-1408-author-local-skills.md :: created.
- history.md :: modified :: appended entry row.

followups:
- the first time each local skill runs, capture any spawned scripts under `codes/` and register their operations in `codes/index.md` (the skills themselves are agent-instructions, not code).
- consider authoring a `file-reading` router skill via `skill-creator` if dataset ingestion becomes repetitive.
- start the first concrete next step from AGENTREAD.md §6: scaffold `codes/` mirroring OCCAM stages (proposals / features / matching / aggregation) and reproduce baseline numbers; this run will exercise `experiment-logger` + `counting-eval` end-to-end.
