# 2026-05-26 13:28 :: bootstrap agent-readable index files

scope: library/, codes/, memory/, AGENTREAD.md (root, not yet edited)

summary:
- Established a uniform agent-readable index format across the repo.
- `library/category.md` rewritten as a structured index of `library/paper/` and `library/scripts/`, with explicit UPDATE_RULES for add/delete/rename of folders/files.
- `codes/index.md` initialized with the same style; each code entry must declare `purpose`, `operations`, `entrypoint`, `usage`, and optional `inputs`/`outputs`. `codes/` is currently empty.
- `memory/index.md` initialized with naming convention `memory/YYYY/MM/DD/HHMM.md`, a memory file template, and rules for when to create a new memory chunk (~every 3-5 user prompts or on topic shift).
- This file is the first memory entry, registering the bootstrap itself.

key_decisions:
- Index files are written for agents, not humans: lowercase keys, fixed fields, one-line entries, no prose paragraphs.
- File-list entry format is unified as `- <name> :: <one-line description>`.
- Code entries must enumerate `operations:` as a verb-led bullet list so agents can pick the right script without reading source.
- Memory files use 24h local time, zero-padded, nested by date (`YYYY/MM/DD/HHMM.md`).
- New memory chunk cadence: every ~3-5 user prompts, on topic shift, or at session end.

files_touched:
- library/category.md :: modified :: rewrote as agent-readable index with UPDATE_RULES.
- codes/index.md :: modified :: initialized agent-readable code index (codes/ still empty).
- memory/index.md :: modified :: initialized memory index with naming + template + rules.
- memory/2026/05/26/1328.md :: created :: this memory entry.

followups:
- Optionally write the same maintenance contract into root `AGENTREAD.md` so any agent entering the repo learns these rules first.
- Decide whether memory files should be flat (`memory/YYYY-MM-DD-HHMM.md`) instead of nested if the daily volume stays low.
